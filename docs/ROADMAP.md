# 路线图

路线冻结：2026-09-08。按验收门槛推进，不预估没有依据的完成日期。

2026-09-08 用户确认：先完成本地开发和回归，再集中做云端部署验收。
不因单项功能完成而提前创建资源或试部署；实际操作云端账号仍需明确授权。

## P0：独立工程与可测试底座

状态：已完成，证据见 `VERIFICATION.md`。

- [x] 独立复制已提交的 ST 源码，不复制用户数据。
- [x] 冻结目标、记录源码和插件提交基线。
- [x] 实现访问保护、CSRF、明确的能力错误。
- [x] 建立 D1 原样存储与合成数据回归测试，25 项基础测试通过。
- [x] 仅从受跟踪源码生成原版静态资源，Workers dry-run 构建通过。
- [x] 本地 D1、Workers 构建和 HTTP 冒烟验证；测试后恢复原本地设置。

退出条件：可重复构建和测试；未配置访问保护时不得泄露资源。
P0 不要求原版 UI 完整启动，不宣称酒馆助手兼容。

## P1：原版前端完整启动与核心存储

状态：进行中，核心存储已通过本地测试，完整退出条件尚未达到。

- [x] 对照 `firstLoadInit`，记录本地原版浏览器的启动请求。
- [x] 版本、设置、密钥状态、预设、角色、世界书和内置扩展发现的基础契约。
- [x] 聊天快照保留元数据、swipe、分支变量和未知字段，真实本地 D1/R2 往返验证通过。
- [x] 角色 PNG 原图存入私有 R2，图片与 API 都经鉴权；默认用户头像及背景可读取。
- [x] 原版 UI 导入合成卡、世界书、预设，角色描述编辑及重新选择后的恢复验证。
- [x] 临时前端 Token 估算：角色/世界书计数、同步/异步调用本地通过，计数零后端请求。
- [ ] 用户朋友的估算方案发布后评估替换；当前不阻塞后续阶段。真实 Token ID 需求随生态用例验证。
- [x] 角色/人设头像上传、替换与浏览器裁剪；原版界面和手机重载验证通过。
- [x] 角色重命名及聊天索引原子迁移、PNG/JSON 覆盖导入保留聊天关联。
- [ ] 批量编辑、角色关联世界书嵌入导出及其他核心契约补齐。
- [x] 原生 JSONL 聊天导入、重命名、管理窗口列表与有界全文搜索；原版 UI 和新手机上下文重载通过。
- [x] 原版前端重建聊天头时保留未传回的扩展字段；改名与重叠保存/删除的冲突保护。
- [ ] 其他聊天导入格式、真正分块存储、恢复入口、跨存储中断与多记录并发删除验证。
- [ ] 大规模聊天搜索与保存性能；目前全文扫描有明确预算，保留聊天头需要读取现有快照。
- [ ] 原版客户端的陈旧写入策略；现有 CAS 仅保护重叠写入和携带 If-Match 的客户端。
- [ ] 首次打开、不支持入口关闭方式、手机端全流程和免费额度下性能验收。

退出条件：原版 UI 可完成角色选择、核心编辑、保存及刷新恢复；
启动流程没有靠假成功掩盖的接口缺口。
当前证据与具体限制见 `P1-CONTRACTS.md`、`VERIFICATION.md`。

## P2：原版提示词与生成链路

状态：进行中。首轮原版界面与本地合成模型生成验证通过，完整退出条件尚未达到。
P1 未完成项继续单独保留，不因进入 P2 而关闭。

- [x] OpenAI/Custom 基础文本接口、模型列表、YAML 参数、加密凭据读取。
- [x] 预设顺序、宏、正则、世界书、Token 预算流程继续复用原实现；计数暂用前端估算，
  明确误差、上下文余量与超限处理，不声称等于提供商计费。
- [x] 本地原版 UI 验证流式中文分片、非流式、错误、停止、中断、缺少结束标记和手动重试。
- [x] 独立凭据选择、禁止重定向和请求头隔离的本地安全测试。
- [x] 2026-09-10 按用户要求取消模型域名白名单，直接使用界面中的 HTTPS 地址；
  174 项本地测试、原版桌面/手机连接与生成回归通过，旧变量残留不要求手动迁移。
- [x] 经用户授权将白名单移除与连接错误提示修复更新到既有云端实例；
  更新前后无提供商调用探针及线上前端资源一致性检查通过。
- [ ] 使用所有者配置的真实提供商验证连接与生成；无网络出站的策略探针不能替代此项。
- [x] 记录合成样例实际出站消息，检查预设/宏/正则/世界书及重生成的一致性。
- [x] 文本后处理、独立 process API、JSON Schema 受限展开、锁定版 reasoning/verbosity 转换。
- [x] 同一批浏览器组装输入经过 Workers 与原 ST 真实路由后，23 份实际请求体相同；
  9 种 process 模式及参数合并/删除顺序对照通过。
- [x] 两个独立前端各自装配固定文本样例：15 个状态检查点、每边 7 份模型请求一致。
  原版使用未修改的锁定源码和自身 Tokenizer；覆盖明确裁剪区间，不声称两种计数的精确阈值相同。
- [x] 原生变量命令、重生成、回复分支 extra、消息编辑、手机全新上下文重载及实际生成事件顺序对照。
- [x] 原生客户端 quiet/raw 生成 API 对照；返回值和模型请求一致，不向聊天追加消息，临时输出预算恢复。
- [ ] 复杂社区必需契约、多回复/工具执行与插件联动。
- [ ] 真实模型、真实 Workers 取消/网络边界与免费额度下资源验收。

本轮接口、限制、验证方式及差异见 `P2-CONTRACTS.md`。

本地固定文本样例的请求和预期变量门槛已通过，具备进入 P3 插件实测的基础；
复杂用例、真实提供商、免费云端与其他未完成项继续保留，P2 不标记为全面完成。

## P3：酒馆助手与 EJS 生态门槛

状态：进行中；锁定版真实插件的首轮本地合成回归通过，仍是发布的强制门槛。

- [x] 固定两插件提交和 ZIP SHA-256；独立显式选择的本地资源包，默认构建不含第三方插件。
- [x] 实读许可文件，保留原 LICENSE、完整源码归档和原编译文件；不运行插件安装/构建脚本。
- [ ] 酒馆助手 AFPL 再分发及与发行项目的许可关系审查；本地能运行不等于允许公开分发。
- [x] 原路径发现、manifest/样式/模块加载、父窗口 API/EJS 桥接。
- [x] 实际脚本 iframe 启用、停用、重启及事件监听清理；实际消息 iframe 按钮更新聊天变量。
- [x] Helper 五类变量保存及全新手机上下文恢复；现有回复分支切换恢复对应消息变量。
- [x] EJS 读取 Helper 变量、直接求值、变量保存；预设/世界书模板在实际模型请求中完成求值。
- [x] Helper 非流式 generate/generateRaw、消息修改和创建；世界书/预设/全局正则编辑、生成联动与重载。
- [x] Helper 流式/按 ID 取消/静默并发/全部停止、两插件在完整原版环境下的独立合成对照；
  18 个状态/事件/请求检查点通过，包括首包前停止、停顿流、原版重生成及手机重载后发送。
- [ ] 其他复杂生成事件与 API；非流式首包前取消、未完整 SSE 事件中途停顿的快速取消边界。
- [x] 用户灯塔 smoke 卡、真实 MVU 与动态 ESM 的双环境功能对照：15 个检查点、每边 5 份请求一致；
  初始化/分支/手机重载/jQuery 按楼层读取通过。卡/runtime 的问题和未通过视觉检查见 `P3-LIGHTHOUSE.md`。
- [x] 收到 card2 源包，独立 v1.1 修订与可复现构建；26 条作者操作、32 个双环境检查点、
  每边 22 份固定回复请求一致；覆盖 EJS 分档、消息计时、递归反例、默认变量清理与面板缺失状态。
  原卡 15 个旧基线检查点再次通过，证据及 T/N 判据修正见 `P3-CARD2.md`。
- [ ] T/N 的真实模型自主填表；其他事件/监听器竞态、变量清理后的恢复、其他社区 MVU 与复杂状态栏。
- [x] 两插件共同启用时的原版流式发送、重生成、中断后恢复、新回复分支及左右切换。
- [ ] 角色切换、导出再导入及其他完整社区场景。
- [ ] 真实免费 Workers 实例、性能与资源记录。

退出条件：兼容矩阵中的必需用例全部通过；边界与不支持行为清楚可见。
实际范围与复现方式见 `P3-CONTRACTS.md`。插件内存在浮动 CDN 依赖，
目前只锁定插件归档，不声称完整依赖离线或完全可复现。P1/P2 未完成项继续保留。

## P4：插件在线安装与更新

状态：进行中。本地核心与原版桌面/手机专项通过，既有云端实例已更新；
云端 API 与运行入口字节检查通过，大包传输和 CPU 预算尚未过关。

2026-09-10 用户调整当前优先级：先把原本地随包预装流程迁入 GitHub Actions，
P4 在线安装后端的资源优化留待后续细化，不删除现有能力，也不提前关闭未通过项。

- [x] 公开 GitHub/GitLab 仓库解析、按真实提交下载、R2 归档与索引、D1 条件切换。
- [x] 原版安装/发现/更新/版本/分支/卸载接口及面板回退按钮。
- [x] 体积、路径、ZIP、鉴权、CSRF、凭据隔离及显式网络错误。
- [x] 当前/上一版保留、并发冲突保护、清理重试及静态包卸载记录，25 项专项单测通过。
- [x] 原版弹窗从真实 GitHub 下载并安装锁定 Helper/EJS；源码哈希和脚本加载通过。
- [x] 合成移动分支更新/回退、桌面/手机刷新及 Helper 全局变量恢复；失败卸载不误报成功。
- [x] 经授权更新既有云端；两款真实插件安装、父提交切换、回退及 R2 运行入口字节核对。
- [x] 中断测试的原 bundled 指针恢复、global 测试副本清理；获得 GraphQL CPU 统计。
- [ ] EJS 完整源码包客户端下载稳定性；单请求整包解析/校验等高 CPU 路径优化。
- [ ] 真实 Workers 免费套餐资源验证；GitLab 真实插件及其他社区扩展。
- [ ] 跨作用域 move、其他托管平台、孤立对象完整恢复流程。

退出条件：可从 URL 安装两个插件并更新、回滚，旧数据不丢失。
具体实现、边界和复现方式见 `P4-CONTRACTS.md`；不因此关闭 P1/P2/P3 的剩余门槛。
本轮云端失败、恢复与资源数据见 `P4-CLOUD.md`，不将“API 调用成功”视为免费资源验收通过。

## P5：个人免费部署与上手

状态：首次所有者云端测试实例已部署，鉴权、基础存储与桌面/手机首屏通过；完整 P5 尚未验收。
2026-09-10 用户明确授权创建三个独立测试资源并部署；不代表 P1/P2/P3 或 P4 已完成。

- [x] 独立测试配置与密钥初始化、离线预检，保留原本地配置；17 项新增测试通过。
- [x] 首次带齐两把密钥的 P3 配置 dry-run，部署手册与云端验收清单；见 `CLOUD-TEST.md`。
- [x] 所有者账号授权、独立 D1/R2 创建、两项迁移、首次带齐 secret 的真实部署。
- [x] 真实云端鉴权/CSRF/基础存储合成回归、R2 私有状态及两插件桌面/手机首屏验证。
- [ ] 真实模型、完整社区卡云端流程及性能/免费额度验收。
- [ ] 当前账号账单套餐确认；OAuth 查询订阅返回 403，未更改订阅或升级套餐。
- [x] 手动 Actions 工作流及配套脚本本地验证：固定插件下载、复用 P3 打包、
  默认 dry-run、既有绑定预检、密钥继承与默认分支上传限制；215 项测试通过。
- [x] 同一构建产物的原版桌面/手机预装启动和合成变量重载通过，未走在线安装或 R2 归档。
- [ ] 推送仓库、配置受限 Token 并实际运行 GitHub 托管 Actions；本地验证不能替代此项。
- [ ] Actions 首次资源/密钥初始化及自动迁移；当前仅支持已有个人实例的更新。

当前 Actions 范围、使用步骤和证据见 `GITHUB-ACTIONS.md`，后端在线安装优化后置。

- 部署按钮、资源自动创建、D1 迁移、所有者初始化。
- 在全新账号验证首次开通流程，记录任何人工或计费资料要求。
- 测量首屏、常用交互、长聊天、复杂卡的请求数、CPU 和存储用量。
- 备份、恢复、升级和资源限额错误均有可执行流程。
- 使用一份不接触开发工具的用户验收清单。

退出条件：新用户能按文档部署、导入并开始使用；额度与兼容范围有实测依据。
