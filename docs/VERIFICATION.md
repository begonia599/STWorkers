# 验证记录

日期：2026-09-08。

## 基线

- Node.js：22.14.0。
- Wrangler：4.129.1，依赖由 `cloudflare/package-lock.json` 锁定。
- ST 和插件提交见 `upstream-lock.json`。
- 原版 `public/` 仅对 `scripts/tokenizers.js` 做前端估算适配，并新增独立估算模块；
  P0 与 P1 初期保持未修改，下文按各轮记录。

## P0 历史验证

- 25 项基础测试通过。
- 数据测试使用内存 SQLite 执行真实迁移和 SQL，不仅使用 Map 模拟存储。
- 安全测试覆盖未配置密码、未登录静态资源、错误认证、CSRF、跨站来源。
- 数据测试覆盖未知字段、聊天元数据、消息变量、swipe 分支及 settings 字符串契约。
- 测试覆盖 1 MiB 限制、无 Content-Length 的流式请求、错误信息脱敏。
- 合成数据不来自原 ST 的私人聊天、角色卡或密钥。
- 原版 591 个受跟踪前端文件打包成功，Worker dry-run 构建通过。
- Wrangler 本地 D1 迁移 `0001_documents.sql` 执行成功。
- 本地 HTTP 冒烟通过：未登录 401、跨站写入 403、设置读写、真实静态资源、
  未实现 API 501、排除能力 410；测试后恢复了原本地设置。
- 根目录 `npm test` 和 `npm run build` 实际执行通过，默认入口指向 Workers。
- 最终资源目录为 593 个文件、28,082,715 字节；包含 Webpack 生成文件和默认数据。
- Worker 包约 10.03 KiB，gzip 约 3.62 KiB；这不代表总前端体积或免费 CPU 消耗。
- 新项目的 `public/`、`src/`、`default/` 和原始依赖锁文件没有差异。
- 本地密码、数据库、构建输出和依赖目录均命中 Git 忽略规则。
- 原 `E:\SillyTavern` 的 Git 工作区保持干净；没有读取或复制其私人数据。
- 没有进行远程部署，没有创建 Cloudflare 云端资源。

## P1 存储轮验证

- 46 项单元/契约测试通过，使用真实内存 SQLite 执行 D1 SQL。
- 最终 Workers dry-run 构建通过：Worker 96.64 KiB，gzip 26.94 KiB；
  静态资源 617 个、39,879,841 字节。大小不代表免费 CPU/请求额度已经满足。
- 最终 bootstrap 检查确认新实例默认 main_api=openai；原版默认源文件没有被修改。
- 新增预设、世界书、角色 PNG/JSON、原版 multipart 重复字段、密钥 API 和聊天接口测试。
- 覆盖 V3 与未知字段、显式 UNSET/null/数组、导入文件名后缀、角色删除是否保留聊天。
- 覆盖超过 1 MiB 的聊天正文、header、swipe、分支变量、完整 JSONL 导出。
- 并发测试确认两个重叠写入只有一个成功；落败快照清理，旧快照不会被失败写覆盖。
- 模拟 R2 写失败、较老快照删除失败、陈旧 If-Match；清理失败不伪装成保存失败。
- 模型凭据测试验证 D1 不含明文，前端状态不含密文/IV，登录密码更换不影响数据解密。
- 独立 `.wrangler/p1-test` 本地 D1 迁移成功，不重置原 8788 实例数据。
- `test:storage-local` 在真实 Wrangler 本地 D1/R2 上通过：
  私有 PNG、聊天保存/读取/导出/冲突、世界书、预设、Worker AES-GCM 密钥 API。
- 浏览器通过原版控件完成首次用户名引导、JSON 角色卡/世界书/预设导入和角色描述编辑。
- 全新浏览器上下文从列表重新选择合成角色后，描述、开场白、世界书、预设及未知字段恢复。
- 开场白 `{{user}}` 由原版前端展开为合成用户名；这不是完整宏或提示词链路验收。
- 本地 Chrome 152.0.7977.76，全新测试上下文，不使用个人浏览器资料。
- 截图使用 1440x1000、390x844；合成角色头像实际加载，手机聊天正文正常换行。
- 启动路由已经返回正确数据，但选择/编辑角色后的 `/api/tokenizers/openai/count` 仍 501，
  原版控制台仍有异常。`fullFrontendAcceptance` 明确为 false，P1 不标记完成。
- 酒馆助手、EJS 没有安装或执行，不因内置扩展加载成功而更改兼容状态。
- `public/`、原版 `src/`、`default/`、原始 package-lock.json 均无差异。
- 原 `E:\SillyTavern` 工作区仍干净；本轮不读取其私人数据。
- 没有远程部署，没有创建云端 D1/R2 或启用计费。

浏览器请求、异常与截图位于 `cloudflare/.build/p1-browser/`。
这些是本地生成证据，不会进入部署资源目录或 Git。
详细接口、大小保护和未完成项见 `P1-CONTRACTS.md`。

## P1 前端估算轮验证

日期：2026-09-08。用户明确接受临时前端估算，等待朋友的方案发布后评估替换。

- 53 项测试通过，新增 7 项估算测试；改动的两个前端文件 ESLint 通过。
- 覆盖 UTF-8 中文、组合字符、补充平面字符、孤立代理项、长文本、空输入、
  消息数组/结构化字段/full 参数、非负结果及准确度元数据；不伪称与精确分词器相等。
- Workers dry-run 构建通过：Worker 96.85 KiB，gzip 27.03 KiB；
  部署静态文件清单 618 个、39,875,058 字节；不包含分词词表或新增 WASM。
- 原版 `tokenizers.js` 改为浏览器估算；算法文件明确列入构建允许列表，
  缓存名包含算法 id，不读取旧精确计数缓存。
- 真实本地 D1/R2 存储回归再次通过，只使用该次唯一命名的合成资料。
- 全新 Chrome 152.0.7977.76 测试上下文通过已有角色、世界书、预设与未知字段恢复。
- 同步/异步估算一致，shadow padding、full 与空消息返回值通过；
  角色总量显示 82、展开后的世界书条目显示 16，均为本轮合成输入的估算值。
- 浏览器请求日志：`/api/tokenizers/*` 请求 0，失败 HTTP 响应 0，未处理页面异常 0。
- 本机 100 次短消息估算约 0.80 ms，仅为本地样例观察，不是手机或免费云端性能保证。
- 桌面 1440x1000 和手机 390x844 截图人工检查通过；头像实际加载。
- 原版后端分词 HTTP API 仍未实现，Token ID 编解码、生成、酒馆助手和 EJS 未通过。
- `fullFrontendAcceptance=false`、`readyForChat=false`，P1 不因这一项通过就标记完成。
- 原 `E:\SillyTavern` 不修改、不读取私人数据；无云端部署或资源开通。

## P1 聊天迁移轮验证

日期：2026-09-08。遵照用户决定，只做本地开发与回归，云端验收留待核心流程完成后集中进行。

- 75 项测试通过，新增 22 项覆盖原生 JSONL、原子改名、管理列表/搜索与聊天头保留。
- 原版契约对照：`src/endpoints/chats.js` 的 import/rename/search，
  以及 `public/script.js` 的导入表单、管理列表、renameChat 和 saveChat。
- 测试覆盖未知头字段、元数据、swipes、swipe_info、消息变量、未知消息字段、
  UTF-8/BOM/CRLF、空白行、坏行、非原生格式、重复导入和大于 8 MiB 的聊天。
- 总 multipart 与完整快照的 16 MiB 上限均测试；保留旧头字段不会绕过快照总量限制。
- 测试覆盖 R2 写失败、D1 提交失败后的清理、目标重名、陈旧 If-Match、
  两个并行改名、旧名保存/删除与改名的交错；原有快照没有被这些冲突损坏。
- 列表空查询不读 R2；词片段匹配规则与扫描预算测试通过，超限返回明确失败而非部分结果。
- 新路由的鉴权、CSRF、来源及方法保护测试通过。
- Workers dry-run 通过：103.86 KiB，gzip 28.61 KiB；
  前端资源仍为 618 个文件、39,875,058 字节，本轮没有修改原版前端文件。
- 真实本地 Wrangler D1/R2 冒烟通过导入、原子改名、目标冲突、读取/导出及合成数据清理。
- 原版 Chrome UI 完成 JSONL 导入、聊天选择、改名、保存；API 导出与保存结果相同。
- 原版前端保存重建聊天头后，自定义头字段仍保留；元数据整体替换，不复活已删除的变量。
- 全新 390x844 浏览器上下文重新选择角色，改名后的聊天、变量和 swipe 数据恢复；
  1440x1000 与 390x844 截图检查通过。
- 新流程失败 HTTP 响应 0、未处理页面异常 0，Token 后端请求 0；
  原有角色/世界书/预设和前端估算浏览器回归再次通过。
- 浏览器使用 Chrome 152.0.7977.76 的临时上下文，不使用个人浏览器资料。
- 当前仍是 `P1-in-progress`，`readyForChat=false`；生成、酒馆助手、EJS 仍未接通或验证。
- 没有创建云端资源、远程部署或提交代码；原 `E:\SillyTavern` 未修改，未读取私人数据。

新浏览器证据位于 `cloudflare/.build/chat-transfer-browser/`；
测试只操作隔离的 8789 实例，自动清理本次唯一命名的合成角色和聊天。

## P1 角色与人设管理轮验证

日期：2026-09-08。仍只做本地开发、迁移和验证，没有云端部署。

- 96 项单元/契约测试通过，新增 21 项头像几何、PNG 限制、角色/人设管理及并发测试。
- 增量迁移 `0002_character_chat_links.sql` 在两个本地 D1 实例成功应用，未重置已有数据库。
- 角色改名把角色和聊天索引在同一 SQL 更新中迁移；R2 对象键、上一快照和聊天时间保留。
  目标有遗留聊天时整次失败；新聊天或头像上传与改名交错时失败写清理，不重建旧名命名空间。
- 测试覆盖头像 R2 写失败、D1 提交失败、清理重试、并行覆盖、人设删除竞争、
  默认头像覆盖/删除墓碑、路径/鉴权/CSRF，以及覆盖导入的未知字段与已有聊天保留。
- 本地浏览器发现并修复 D1 changes 包含触发器写入导致的成功后误报 409；
  内存 SQLite 测试替身改用 total_changes 差值对齐，并加入专门回归。
- 原版上传调用处增加小范围适配；没有增加后端图片处理库或改写原版界面。
  四个本轮前端 JS 文件通过 ESLint。
- 最终 Worker dry-run 通过：110.16 KiB，gzip 29.72 KiB；
  部署静态清单 619 文件、39,878,386 字节。仍不代表免费 CPU/请求/存储额度验收通过。
- Chrome 152.0.7977.76 临时上下文执行原版角色 JPEG 裁剪、改名、JSON 覆盖导入；
  已使用聊天的未知头字段、变量、swipes、swipe_info 和消息扩展保留。
- 原版人设控件完成上传、命名/描述、同文件名替换；全新 390x844 上下文恢复名称、
  描述、未知人设字段与图片，再由原版按钮删除。
- 图像解码检查确认裁剪为 512x768；合成绿色区域中心为 [32,192,96,255]，
  替换后的红色区域中心为 [229,32,49,255]。不裁剪的浏览器转换保留 1200x900 尺寸。
- 取消角色裁剪后再执行原版保存，不会带上未确认的图片；浏览器用例已覆盖。
- 本轮成功的浏览器流程：失败 HTTP 响应 0、未处理页面异常 0、后端 Token 请求 0。
- 本轮浏览器证据在 `cloudflare/.build/character-management-browser/`；
  1440x1000 桌面与 390x844 手机截图已检查。脚本清理合成角色/人设并恢复隔离测试设置。
- 真实本地 Wrangler D1/R2 存储冒烟再次通过，未操作所有者数据。
- 原有角色/世界书/预设/Token 浏览器回归，以及 JSONL 导入/聊天改名/手机重载回归均再次通过。
- 状态仍为 `P1-in-progress`、`readyForChat=false`。批量编辑、完整恢复和并发策略、
  模型生成、酒馆助手/EJS、真实免费资源验收仍未完成。
- 原 `E:\SillyTavern` 工作区干净；未读取其私人数据，未创建远端资源、部署或提交代码。

## P2 基础生成轮验证

日期：2026-09-08。阶段为 `P2-in-progress`；本轮没有调用真实模型、创建资源或部署云端。

- 116 项单元/契约测试通过，新增 20 项生成、URL/请求头、凭据、YAML、流和异常回归。
- 原版 `openai`、`custom` 请求可进入独立 Worker 生成模块；没有导入原版 Node 后端。
- 普通消息和参数投影、tools 传输、YAML 合并/删除、独立密钥读取及 secret_id 选择通过单测。
- 允许列表、HTTP/IP/敏感地址、URL 凭据、重定向、鉴权/CSRF、浏览器头隔离和错误脱敏通过。
- 五分钟超时、取消信号、输出大小、非法 JSON/响应结构、流异常和中文跨字节分片测试通过。
- `public/scripts/openai.js` 仅修改错误抛出及 OpenAI/Custom 结束标记检查；ESLint 通过。
  后者运行在浏览器，Worker 不解析 SSE 事件，也不进行 Token 分词。
- 最终 dry-run：325.84 KiB，gzip 78.63 KiB；增长主要来自固定版本的 YAML 解析依赖。
  静态清单 619 个文件、39,878,514 字节。未作免费 CPU/内存/请求配额承诺。
- 原版 UI 和真实本地 Workers/D1/R2 对接本地合成上游，发出 11 次合成生成请求。
- 第一轮实际消息包含命名预设、关键词世界书、角色描述；用户/角色宏正确展开。
  UI 中保留 RAWWORD，模型收到 REGEXWORD；温度 0.37、seed 42 保留。
- 重新生成的实际消息数组与首轮相等；停止按钮在本地确实断开上游连接。
- 非流式、429 后手动重试、流异常中断及缺少 `[DONE]` 的正常关闭均得到验证。
  错误后可手动重新生成，不由 Worker 自动重试。
- 等待真实聊天快照保存后，全新手机上下文恢复 13 条聊天，再连接合成模型并完成手机发送。
- 1440x1000 桌面、390x844 手机截图已经检查；手机无横向溢出，最终回复在可见聊天区域。
- 后端 Token 请求 0；只有故意注入的 HTTP 429。
  原版初始请求的点击处理产生一条预期页面异常，其精确内容为
  `The model service rate limit or quota was exceeded.`；没有其他未预期页面异常。
- 浏览器证据：`cloudflare/.build/generation-browser/`。测试数据在 `.wrangler/p2-test`，
  8790/8791 服务仅在脚本执行期间启动，结束后关闭。未读原项目私人数据。
- 真实本地 D1/R2 存储冒烟再次通过，已有数据和所有者设置不受影响。
- 原有三组浏览器回归串行再次通过：角色/世界书/预设/Token、聊天导入/改名/手机重载、
  角色/人设头像/覆盖导入。与生成套件并行的首次旧套件运行曾出现一次 20 秒页面加载超时；
  随后单独重跑通过。没有据此声称性能门槛已经通过。
- 这不是原版 Node ST 双实例对照，不构成 EJS、酒馆助手、真实模型或实际社区卡兼容证据。
- `readyForChat=false` 继续表示整体验收门槛尚未通过，不再表示基础生成接口不存在。

剩余接口与安全约束见 `P2-CONTRACTS.md`；P1 缺口、插件及真实云端验收继续保留。

## P2 后处理与原路由对照

日期：2026-09-08。继续保留 `P2-in-progress` 和 `readyForChat=false`。

- 总计 132 项单元/契约测试通过，本轮新增 16 项后处理、Schema、预算及原版对照测试。
- 导入锁定 ST 的实际 `postProcessPrompt`/`mergeMessages` 函数，108 个固定组合、
  720 个确定性混合序列与移植实现相同；移植函数不修改调用方输入。
- 对照前检查核心源码及公开默认配置与 `upstream-lock.json` 指定提交一致。
  没有读取原项目私有配置、角色、聊天或模型密钥。
- 原版 UI 下拉控件逐一切换 8 个后处理选项后发送，正常返回；历史 claude 别名经 API 对照。
- 独立 `/api/backends/chat-completions/process` 的 9 种模式返回与原版相同；
  非数组输入、未知模式、错误方法、鉴权与 CSRF 测试通过，不调用上游模型。
- JSON Schema 的根 $defs 展开、循环/缺失引用、引用兄弟字段、外部引用和未知字段对照通过。
  深度 64、访问节点 20000、保守 1 MiB 展开边界及原型键安全检查通过。
- reasoning/verbosity 映射与锁定 ST 表一致；不把该表描述为当前模型可用性名单。
- 修正上一轮 Custom YAML 与 stop/tools 的覆盖优先级，Schema/推理参数转换和 exclude 顺序对齐原版。
- 同一份浏览器组装输入分别经本地 Workers 和原 ST 的真实 chat-completions 路由，
  在合成模型端捕获的 23 份请求体逐字段相同。其中含 19 份 UI 请求与 4 组 Schema/高级参数请求。
- 参照服务使用随机回环端口与随机测试凭据，只开放三个所需路径并限制唯一合成模型地址；
  空数据根位于 `.build/reference-api/`，不启动完整 Node ST 服务或对外开放原后端。
- 原版路由只进入测试进程，部署 bundle 未包含原 `prompt-converters`、Express 或 Node 后端。
- 最终 dry-run 为 335.29 KiB，gzip 80.88 KiB；静态资源仍为 619 文件、39,878,514 字节。
  本轮没有修改前端文件，没有新增生产依赖，没有声称免费 CPU/内存门槛已通过。
- 最终浏览器回归再次覆盖发送、停止、429、断线、缺少结束标记、恢复、手机重载及发送。
  后端 Token 请求仍为 0；只有明确注入的 429 及对应原版点击处理异常，没有其他未预期错误。
- 最终真实本地 D1/R2 存储冒烟再次通过。8788/8789 预览恢复；临时生成/参照测试服务关闭。
- 没有云端部署、远程资源创建、真实模型调用或提交代码；原 `E:\SillyTavern` 工作区未修改。

证据：`cloudflare/.build/generation-browser/results.json`、`parity.json` 及桌面/手机截图。
`parity.json` 含每一份合成输入和两端实际模型请求体。
这是两个实际后端的对照，不是两个独立前端的完整装配/上下文边界/变量对照，
也不代表工具执行、酒馆助手、EJS 或真实社区卡兼容已经完成。

## P2 独立前端、变量与脚本生成对照

日期：2026-09-08。本地固定文本样例的 P2 进入插件阶段门槛已通过；
完整 P1/P2、插件与云端资源验收仍未完成，继续 `P2-in-progress`、`readyForChat=false`。

- 新增独立前端回归与原版参照启动器，本轮没有修改生产前端、生成逻辑或新增依赖。
- 原版从锁定提交导出 904 个受跟踪源码文件；关键前端/启动文件逐字节哈希校验通过。
  Git 导出单次指定 `core.autocrlf=false`，没有更改项目或全局 Git 配置。
- 启动完整原版 ST，使用新数据根、随机 Basic 凭据和原生 CSRF，只监听 127.0.0.1。
  没有复制私人配置、用户数据、忽略的扩展或原项目依赖目录。
- Workers 使用独立 `.wrangler/p2-parity/<uuid>`；两端各自从原生 API 初始化相同合成资料。
  浏览器会话、设置、聊天、变量和模型请求各自独立，不重放另一端组装好的输入。
- 15 个状态检查点通过，两端各 7 份实际模型请求逐字段相同。
  包括卡片/预设/正则/世界书/宏、原生变量命令、重生成、切换回复、编辑及手机重载。
- 聊天元数据、全局变量、卡片未知字段、消息未知字段及回复分支 extra 的已列语义字段保留。
  分支字段由合成测试通过原生客户端 API 写入，不是酒馆助手或 MVU 执行证据。
- 8192 上下文保留合成旧长消息；1024 上下文剔除该旧消息并保留最近消息；
  数据库存储中的旧长消息保持完整。两端使用各自 Tokenizer，没有替换原版计数。
- 整段样例计数分别为 Workers 4481、原版 1770；裁剪后分别为 186、173。
  这是合成长重复文本上的差异记录，不是提供商账单，也不证明临界阈值一致。
- 原生 `generateQuietPrompt`、`generateRaw` 返回值及模型请求一致，不改聊天消息；
  临时输出上限 64 实际发出后恢复为 128，Raw 不附带卡片/预设/世界书。
- 回复 `setvar` 在本例显示时不执行，包含到后续提示词后执行，与原版一致。
  测试只使用幂等宏赋值；复杂和非幂等宏仍需专项测试。
- 真实生成/消息/切换回复的事件顺序一致。带明确 dryRun 标记的后台面板试算单独记录；
  防抖与执行速度造成的试算次数差异不被误报为模型生成顺序不同。
- 关闭旧会话后，两套全新 390x844 手机浏览器都恢复变量、分支和消息，并继续发送。
  1440x1000 桌面与 390x844 手机截图检查通过，手机没有横向溢出。
- 最终构建上双前端套件再次通过；浏览器 HTTP 失败 0、未处理异常 0、站外请求 0。
  Workers 后端 Token 请求 0；原版真实调用自身本地 Tokenizer。
- 更新状态接口的固定样例验收标记；132 项单元/契约测试通过，`git diff --check` 通过。
- 最终 dry-run：335.43 KiB，gzip 80.92 KiB；静态资源仍为 619 文件、39,878,514 字节。
  部署 bundle 未包含参照服务器、Express 或 `node:fs`。
- 真实本地 D1/R2 存储冒烟通过；只清理唯一命名合成资料，不修改所有者设置。
- 8788/8789 原预览已恢复，8792/8793/8794 临时服务已关闭。
  测试输出、原版参照目录与配置都处于 Git 忽略目录。
- 没有调用真实模型、云端部署、创建远端资源或提交代码，原 `E:\SillyTavern` 工作区未修改。

证据位于 `cloudflare/.build/frontend-parity/`：
`results.json` 含状态、事件、后台试算及计数；`requests.json` 含两边独立的实际请求。
此证据允许下一阶段直接推进真实插件加载与 API 用例，不能替代酒馆助手、EJS 或实际社区卡门槛。

## P3 真实插件首轮本地回归

日期：2026-09-08。进入 `P3-in-progress`，继续 `readyForChat=false`。
锁定版真实插件已有本地合成用例通过，不等于完整生态验收。

- Helper 4.9.5：`8e0f4324e7d051025a333831411f03bd3145fac8`；
  EJS 1.17.9：`d6f520d149aba146305b0b781ddd691d449c28d2`。
  公开归档的 SHA-256、来源、许可和复现方式见 `P3-CONTRACTS.md`。
- 新增显式选择的独立资源打包路径，保留原插件编译文件、目录、LICENSE 和完整源归档。
  默认构建没有第三方插件，没有执行其安装或构建脚本，也没有读取私人插件目录。
- 增加 6 项插件资源/ZIP 测试，总计 138 项通过；路径、大小写重名、Unix 符号链接、
  源码根、归档哈希及运行资源选择均有断言。
- 原版前端真实发现、初始化两插件；未认证 manifest、编译入口、LICENSE 和源 ZIP 均为 401。
- Helper 实际 API 写入全局、预设、角色、聊天、消息五类变量，全新手机会话恢复；
  EJS 直接求值读取这些全局/聊天值，并保存新的聊天变量。
- Helper 修改世界书、命名预设及全局正则，实际发给合成模型的提示词包含修改和模板结果；
  不残留 EJS 语法或应被正则替换的标记。手机新会话恢复资源编辑及已断言的未知字段。
- 原版 UI 发送、Helper 非流式 generate/generateRaw 共捕获 3 个实际模型请求；
  两个脚本生成返回正文，不向聊天添加消息。
- Helper 改写消息、写入两个已有回复分支，原版左右按钮恢复对应消息变量。
  原版切换动画后再进行下一次操作，未通过修改变量绕过 UI 验证。
- 实际全局脚本 iframe 经过启用、停用和再启用：挂载 2 次、pagehide 1 次、事件命中 2 次；
  停用后旧监听未再触发，父窗口 API/EJS 桥接正常。
- Helper 创建的真实 HTML 消息 iframe 按钮从 0 改为 1；关闭桌面会话后，
  全新 390x844 手机上下文恢复并继续改为 2。1440x1000 桌面与手机截图检查通过，无横向溢出。
- 真实 CDN 依赖本轮成功响应；测试只放行插件源码中的精确依赖 URL，其他外链视为失败。
  凭据限定本地 origin。没有本地 mock 这些库，也不声称浮动 URL 已被锁定或可离线使用。
- 最终本地 HTTP 失败 0、未处理页面异常 0、未预期控制台错误 0，后端 Token 请求 0。
  原版预设切换的 `AbortReason` 按类型、来源和具体原因单独记录，未屏蔽任意错误。
- 修复原页面缺失 `css/user.css` 的 MIME 报错；生成无用户规则的占位资源，不复制私人样式。
- 默认与独立 P3 dry-run 均通过：Worker 335.69 KiB、gzip 81.02 KiB。
  默认资源 620 文件、39,878,558 字节；P3 资源 742 文件、77,355,343 字节。
  未把插件代码、ZIP 解析器、参照服务、Express 或 `node:fs` 放进 Worker bundle。
- 最终构建后 P3 浏览器回归再次通过；独立本地 D1/R2 存储冒烟通过，
  只创建/清理唯一命名合成资料，未修改所有者设置。
- 8788/8789 已恢复，新增独立 8795 P3 预览；三者均只监听 127.0.0.1。
  只读检查确认默认实例发现 2 个内置扩展，P3 实例发现 4 个扩展。
  8796/8797 临时服务已关闭。
- 没有新增生产依赖、改写上游插件、调用真实模型、部署云端、公开发布或提交代码。
  原 `E:\SillyTavern` 工作区仍未修改。

证据：`cloudflare/.build/p3-browser/results.json`、`desktop.png`、`mobile.png`。
结果含插件提交、11 个阶段、实际模型请求、变量、资源、生命周期与外部请求。
Helper AFPL 再分发审查、真实社区卡/MVU、其他 API、独立原版插件对照和云端均未完成。

## P3 独立原版流式与重生成专项

日期：2026-09-09。保持 `P3-in-progress`、`readyForChat=false`；真实 MVU 卡由用户准备。

- 原版 ST 与 Workers 各自初始化完整合成卡、Default 预设和同一锁定版真实插件。
  参照启动器新增显式 pluginBundle 选项，默认路径不安装插件；关键原版前端仍逐字节验证。
- 18 个状态/事件检查点通过，两边各 18 份实际模型请求体逐字段相同。
  包括 Helper 普通/Raw/自定义 API 流式、按 ID 停止和复用、重复 ID 拒绝、
  首包前停止、按钮绑定与静默并发、stopAll 后恢复、延迟错误，以及原版重生成/分支/手机。
- EJS 预设/Raw 求值不残留模板标记，非幂等计数相对初值递增一次；
  回复变量继承后只递增一次。手机第二次回复从 1 到 2 与原版一致，不是重复触发。
- 记录真实 Helper 和原生事件顺序；不强制异步 before_end 观察者先于 ended，
  不把最后一次 full/空 incremental 当成重复结束，也不屏蔽原版已有的两次无 ID 停止通知。
- 修复流式首包等待和完整事件间停顿时上游连接不能及时关闭的问题。
  流头等待 250 ms 后打开 SSE，安全事件边界按秒注释保活；不解析模型 JSON、不做后端分词。
  慢流的错误在已发送 HTTP 200 后用脱敏 SSE error 表示，无成功结束标记和自动重试。
- 修复前，独立 Miniflare 和直连 workerd 均不能在五秒观察窗内关闭首包前请求；
  修复后的最终构建包两条路径均关闭，真实插件双环境也通过首包前及停顿流停止。
  非流式首包前、未完整 SSE 事件中途停顿的快速取消和真实云端仍未验收。
- 共 146 项测试通过，本轮新增 3 个合成模型测试和 5 个流式等待/错误/保活/取消测试。
  SSE 跨 UTF-8 分片、CRLF 跨片、无分片内插入、待处理 read 取消均有断言。
- 故意注入两次延迟 429：原版两条 429 和对应两条资源控制台错误精确单列；
  Workers 通过 SSE 明确拒绝。其余 HTTP 错误、控制台错误、未处理页面异常均为 0。
- Workers 后端 Token 请求 0；独立原版 57 次原生 Tokenizer 请求，没有替换参照计数器。
- 两边全新手机上下文恢复消息、变量、swipes 和流式开关，再次发送正常；
  1440x1000 桌面与 390x844 手机截图已检查，中文正文无模板标记、无横向溢出。
- 原 P2 生成/错误/手动重试/后处理及真实路由对照回归通过；原 P3 变量/资源/iframe 基线通过。
  真实本地 D1/R2 存储冒烟通过，只清理唯一命名合成资料。
- 无第三方插件的独立双前端基线再次通过：15 个状态、两边各 7 份模型请求一致；
  原版参照启动器不带 pluginBundle 时维持原流程，变量、上下文裁剪、quiet/raw 和手机恢复没有回归。
- 默认与显式 P3 dry-run 通过：337.79 KiB、gzip 81.54 KiB。
  静态清单保持默认 620 文件/39,878,558 字节、P3 742 文件/77,355,343 字节。
  新逻辑在 Workers 生成模块，不修改原版前端、第三方插件或增加生产依赖。
- 原 `E:\SillyTavern` 工作区未修改；没有真实模型调用、云端资源创建、部署、发布或提交。
- 8788/8789/8795 本地预览已恢复，三者未认证 401、认证状态 200；
  均继续报告 `P3-in-progress`、`readyForChat=false`。测试证据及本地密钥仍被 Git 忽略。

本轮通过证据：`cloudflare/.build/p3-generation/2d34c557-c6fb-4bdb-a1fb-f84c96bf661a/results.json`
及同目录截图；后续以 `latest.json` 指向的最新执行为准。
直接运行时取消证据在 `cloudflare/.build/cancellation-runtime/results.json`。
复现、异步事件语义和测试卡要求见 `P3-CONTRACTS.md`，传输差异见 `P2-CONTRACTS.md`。

## 用户灯塔卡与真实 MVU

日期：2026-09-09。具体基线、输入哈希、卡/runtime 问题及复现见 `P3-LIGHTHOUSE.md`。

- 只导入用户明确提供的非敏感 JSON，原卡不变；作者 lint、自模拟和未收到的 T/N 表没有冒充本轮结果。
- 两个独立原版/Workers 浏览器加载相同真实 MVU bundle 和同次缓存的 CDN 依赖，无 Mvu/API 桩。
- 15 个状态/观察检查点、每边 5 份合成模型请求一致，覆盖初始化、发送、重生成、swipe、EJS、
  世界书正反例、正则、手机重载与 jQuery 逐楼层变量。HTTP/页面/控制台/未授权外部请求错误均为 0。
- 原卡存在动态物品键不能新增、备用 initvar 覆盖整表和旧值检查假设不成立；
  delete 命令可执行，只读/范围不是本卡代码强制约束。均在原版复现。
- 纯宏面板重载后显示最新值，但历史变量没有丢；jQuery 按楼层取值正确，
  透明面板黑字在深色主题下不可读，视觉问题保留，没有改原卡掩盖。
- 实际事件轨迹单独记录，部分阶段观察顺序/预览次数不同，尚未归因。
  不声称完整事件一致或真实模型填表通过，P3 与 readyForChat 标记不提升。
- 单元回归 146 项通过，默认构建/dry-run 通过，Worker 337.79 KiB、gzip 81.54 KiB。
  首次构建因旧预览占用 assets 目录报 EBUSY，核实进程后临时停 8788/8789 重跑成功；
  两个服务按原参数恢复，8795 未动，三者未认证 401、认证 CSRF 200。
- 本轮只新增测试脚本和文档，无生产源码、原版前端、插件或原始卡修改，无云端部署或真实模型费用。
  8801/8802/8803 测试服务在 finally 关闭，独立数据/证据不进入部署资源。

本轮最终成功证据：`cloudflare/.build/p3-lighthouse/1b7074bb-d65f-411b-bd34-5e66961b52c3/results.json`。
一次 CDN 超时导致的复跑失败另行保留，随后原样重试成功；不当作从未发生。
证据索引：`cloudflare/.build/p3-lighthouse/latest.json`，原始状态、请求和截图在对应运行目录。

## card2 源包与 v1.1 修订

日期：2026-09-09。保持 `P3-in-progress`、`readyForChat=false`，详见 `P3-CARD2.md`。

- 用户 card2.zip 已安全审查、解包，原卡哈希与前轮相同，8 个 JSON 解析与结构检查通过。
  作者个人 lint/configure/verify/build 工具不在包内，未访问其私有技能目录或冒称重跑。
- 独立 v1.1 修订补齐备用初值、可扩展物品栏和 insert、逐楼层深色面板与缺失快照提示；
  删除错误的旧值拒绝保证。原卡/原 ZIP 保持不变，没有改 ST、插件或 MVU 来迁就卡。
- 标准库 build.mjs 锁定原始输入和 policy/types，保留布局与未知字段，
  重复构建得到相同修订卡字节和 sourceHashes，不替代未提供的配置器。
- 32 个独立原版/Workers 检查点、26 条作者操作、每边 22 份固定回复请求一致。
  主/备用初始化、set/insert/delete、EJS 三档、分支与手机重载发送均覆盖。
- sticky/cooldown/delay 按 2/4/6 条扫描消息实测；递归反例保留 probability=0 和 delay 抑制。
  T6 只做不落盘的软约束边界探针，不冒充真实模型拒绝测试。
- MVU 默认清理最近 20 楼以外的旧变量，第 25 条消息的清理位置与原版一致；
  快照缺失不显示为 0。没有关闭清理，变量恢复继续留待专项。
- 分开记录 emit 调用与晚注册监听器回调；保留节流/异步等待导致的失败和观察，
  不把回调日志当作发出时序，也不扩大成任意监听器的时序保证。
- 使用前轮已记录的 29 项真实 CDN 依赖，逐项哈希验证重放；没有 API/引擎桩，
  不声称本轮 CDN 可达或生产依赖离线。清洁回归的 HTTP/页面/控制台/外部请求错误均为 0。
- 原卡旧基线再次通过：15 个检查点、每边 5 份固定请求，仍保留原卡已知问题。
- 146 项单元/契约测试通过；默认资源构建后的独立 dry-run 为 337.79 KiB、gzip 81.54 KiB。
  本轮只有测试、卡副本和文档修改，没有新增生产依赖或把测试资源打进部署包。
- 没有调用真实模型、部署云端、创建资源、公开发布或提交 Git，原酒馆未改动。

首轮修订卡成功记录：`cloudflare/.build/p3-lighthouse/1a3b2059-87ce-428d-ab65-a18b34263b54/results.json`。
最终严格时序/实际 MVU 完成等待与完整截图回归：
`cloudflare/.build/p3-lighthouse/a0da2b5f-43be-4c0c-9f18-2b8f6c156612/results.json`。
32 项发出时序断言通过，最终 Token 请求 Workers 0、原版 247。
原卡复跑记录：`cloudflare/.build/p3-lighthouse/fe5e3244-d177-4730-831e-c673bd3de83c/results.json`。
最终时序断言、截图和失败解释以 `P3-CARD2.md` 的执行索引为准。
原 8788/8789/8795 预览未认证 401、认证 CSRF/状态 200，readyForChat 仍为 false。

## 首次云端测试的本地准备

日期：2026-09-10。继续保持 P3 未全面验收、`readyForChat=false`；
仅完成 P5 的测试准备子项，没有登录 Cloudflare、创建资源或上传。

- 新增 `setup-cloud.mjs`、`check-cloud.mjs` 和纯本地配置模块。
  独立配置位于 `.deploy/stworks-cloud-test/`，不属于 `.build/` 或静态资源。
  两个文件经 `git check-ignore` 验证被忽略；源码尚未提交，与既有工作区状态一致。
- 云端草案已生成新的 `AUTH_PASSWORD` 与 32 字节 `DATA_KEY`，
  与本地两项秘密逐项比较不同。重复初始化后，本地/云端配置和密钥四个文件均逐字节不变。
  生成工具不读取 `.dev.vars`，本轮单独的只读验证只报告比较结果，没有打印密钥。
- 为独立 D1/R2 固定测试名称；账号/D1 ID 尚未知，保留明确占位符。
  普通预检以退出码 1 拒绝占位符，`--draft` 明确返回 `cloudVerified=false`，
  不将本地成功描述成远端资源存在或账号配置完成。
- 新增 17 项自动测试，总计 **163 项通过**。
  覆盖名称和 ID 校验、模型 origin、鉴权配置保护、密钥格式、重复/冲突/中断初始化、
  并行初始化、符号链接、P3 清单、资源大小、秘密误入资源与错误信息脱敏。
- 初次实际命令验证发现本机 `npm.ps1` 吞掉转发参数，误选无插件目录。
  文档改为直接 Node 命令，实测 `npm.cmd` 也能传参；只修正草案资源选择，没有重置密钥。
  预检原先拒绝原版受跟踪的空 `.gitkeep`；现仅放行其确切路径且要求零字节，新增回归测试。
- P3 草案实际资源预检通过：742 个文件、77,355,343 字节，最大文件 13,622,195 字节；
  Helper 4.9.5、EJS 1.17.9 的名称/提交与构建清单和源码锁一致。
  这是库存和关键配置检查，不是全部文件的密码学完整性证明或第三方 CDN 可达性测试。
- 必需的普通 `npm --prefix cloudflare run build` 通过：
  620 个静态文件、39,878,558 字节，Worker 337.79 KiB、gzip 81.54 KiB。
- 独立配置携带 `--secrets-file` 的 P3 `wrangler deploy --dry-run` 通过：
  Worker 338.58 KiB、gzip 81.56 KiB，绑定 DB/FILES/ASSETS 与两项隐藏的 secret。
  本地产物在 `.build/cloud-test-worker/`，逐文件检查不包含云端两项秘密的值。
  Wrangler 扫描日志计数还包含目录条目；上述文件数采用文件清单和逐文件遍历。
- 新增 `CLOUD-TEST.md`：分开写明本地准备、账号/计费确认、独立资源创建、
  迁移后首次带齐密钥上传，以及认证/存储/真实模型/MVU/手机/资源消耗验收。
  远端命令只存在于待授权手册，本轮没有执行；没有调用真实模型或产生模型请求费用。
- 本轮未修改生产 Worker 源码、原版前端或插件，也未启动或停止预览服务。
  未重跑浏览器、灯塔引擎和真实本地 D1/R2 冒烟；其已有证据仍以之前各节为准，
  不用这次工具测试替代新的浏览器或云端验收。`git diff --check` 通过。

复现本轮新增本地检查：

```powershell
node cloudflare/scripts/setup-cloud.mjs --with-p3-plugins
node cloudflare/scripts/check-cloud.mjs --draft
npm --prefix cloudflare test
```

没有账号/D1 ID 时，不带 `--draft` 的检查**应当失败**。
真实上传前还需所有者确认账号、R2 开通/费用条件并授权远端操作。

## 首次真实云端部署

日期：2026-09-10。用户明确授权创建独立资源并部署测试版。
本轮只验证首次部署、鉴权、基础存储和原版首屏；P3 仍未全面验收，`readyForChat=false` 不变。

- `whoami` 确认 OAuth 登录，仅关联一个账号；三个目标名称在创建前均无冲突。
  只创建 `stworks-cloud-test`、`stworks-cloud-test-db`、`stworks-cloud-test-files`，
  未改动其他项目，也未创建公网 R2 域名或追加其他云服务。
- 账单核验的明确限制：Workers account-settings 返回 `default_usage_model=standard`，
  订阅 API 返回 403 / 10000，当前 OAuth 没有账单订阅读取权限。
  没有执行订阅或升级操作，但不能将此写成“已证实 Free 套餐”或“已证实零费用”。
- 新建 D1 与 R2 均在 APAC；R2 明确指定 Standard。
  两项迁移 `0001_documents.sql` / `0002_character_chat_links.sql` 在新 D1 成功执行。
  原本地 `wrangler.jsonc`、`.dev.vars` 和独立云端密钥均未改动，只给云端配置补充真实账号/D1 ID。
- 上传前 163 项本地契约测试再次通过；普通构建/dry-run 通过，
  Worker 337.79 KiB、gzip 81.54 KiB，普通资源 620 文件/39,878,558 字节。
  P3 配置正式离线预检通过，资源清单 742 文件/77,355,343 字节。
- 实际首次上传同时携带 `AUTH_PASSWORD` 与 `DATA_KEY`，没有先裸部署再补鉴权。
  上传日志记录 740 个文件上传、2 个已上传文件；静态资源上传约 269 秒。
  云端 Worker 338.58 KiB、gzip 81.56 KiB，部署工具报告 Startup Time 12 ms；
  这是启动指标，不是每请求 CPU 用量或免费额度性能结论。
- 官方控制面再次只读确认：两项绑定类型都是 `secret_text`；
  Worker 正式 workers.dev 地址启用、预览地址关闭；
  R2 Standard，管理公共域名 `enabled=false`，公开自定义域名列表为空。
- 真实 HTTPS 冒烟共 39 个请求：未认证 HTML/脚本/引导数据/插件/角色图片均为 401；
  错误密码为 401，正确认证返回原版页面、阶段信息和两插件 manifest/发现数据。
  缺少 CSRF、跨来源写入均为 403。
- 合成 JSON 卡导入和 R2 PNG 头像读取通过；聊天保存/读取/JSONL 导出/改名、
  swipe、变量和未知字段完整往返；陈旧 revision 为 409。
  世界书/预设往返通过；假模型凭据的 AES-GCM 写入及掩码读取通过，明文读取拒绝。
- 仅创建唯一命名的合成资料；四项清理 API 分别为 200/200/200/204。
  HTTP 冒烟中所有者设置字符串保持不变，没有触发任何模型服务请求。
- 两套隔离无头 Chrome 会话真实打开云端：1440x1000 桌面、390x844 手机。
  原版欢迎向导、Helper/EJS 全局入口加载通过；均未代替所有者完成初始人设设置。
  截图已人工检查，无横向溢出，HTTP 失败/未处理页面异常/控制台错误均为 0。
- 两个会话分别记录 268/269 个浏览器请求，后端 Token 请求均为 0。
  从导航到插件入口和欢迎页就绪约 40,671/26,574 ms，首次加载偏慢，性能门槛未通过。
  这是隔离自动化会话下的记录，不是热缓存基准，也未测真实长聊天或 MVU。
  手机会话的 Helper GitLab manifest 查询返回 200；不能扩展成其他浮动 CDN 全部可达的保证。
- 模型连接/生成接口在浏览器检查中被显式禁止，实际没有触发拦截，也没有真实模型调用。
  本轮未修改 Worker 生产源码、原版前端或第三方插件，未启动或停止本地预览。
  没有提交 Git。真实模型、复杂社区卡云端行为、CPU/额度及账单套餐继续待验证。

实例 URL、版本 ID、账号/D1 标识仅保存在 Git 忽略的
`cloudflare/.deploy/stworks-cloud-test/deployment.json`，不把个人云端入口写进公共源码。
可重复执行的本轮操作脚本与证据放在被忽略的 `.build/cloud-first-deploy/`：
`http-results.json`、`browser-results.json`、`desktop.png`、`mobile.png`。
其中脚本仅针对已授权的测试实例，不是通用部署自动化，也不能用于任意新目标。

## 模型连接错误提示修复

日期：2026-09-10。用户报告云端连接模型时 status 接口 403，控制台只显示空 Error。
本轮仅修复本地前端提示，没有调用云端接口、读取用户模型密钥或修改部署配置。

- 源码定位：`getStatusOpen()` 只抛出 `response.statusText`，没有读取结构化错误正文；
  Custom 默认允许跳过模型列表检查，catch 不会清除该状态，也没有显示通知。
- 新增 8 项直接执行实际 `getStatusOpen()` 的合成回归。修改前 7 项失败、1 项通过；
  修改后 8 项全部通过。覆盖空 statusText、白名单/CSRF/未适配来源、HTML/损坏 JSON、
  200 错误封装、纯文本通知选项、请求/响应体取消，以及成功与模型列表跳过的原行为。
- 修复将错误 HTTP 状态、code、message 显示为转义文本；安全/配置错误不能继续显示为跳过检查。
  原有上游模型列表不可用时的 Custom bypass 保留，但错误仍明确显示；没有自动重试或生成测试消息。
- 全部 **171 项本地测试通过**；前端 `node --check` 通过。
  必需的普通构建与 Workers dry-run 通过：337.79 KiB，gzip 81.54 KiB。
- 初始独立云端配置仍为 `MODEL_ALLOWED_ORIGINS=[]`，除官方 OpenAI 外的 origin 会被拒绝；
  用户尚未提供模型地址或响应错误码，不能把这次实际 403 的根因写成已确认。
- 未上传修复、未重建 P3 插件资源、未修改云端绑定或密钥、未调用真实提供商；
  尚未验证浏览器实际通知效果或云端连接恢复。P1/P2/P3/P5 状态不因此完成。

## 模型域名白名单移除

日期：2026-09-10。用户明确要求面向新手取消模型域名白名单。
本轮完成本地实现、测试与部署包准备，没有更新已经部署的云端 Worker。

- 从生成后端删除域名允许列表及官方 OpenAI 特例；Custom 与 OpenAI 反向代理直接使用
  界面中的 HTTPS API base URL。没有另加审批、白名单开关或部署参数。
- 保留鉴权、CSRF、请求头/凭据隔离、禁止重定向、超时与大小限制。
  地址格式检查继续拒绝 IP、保留本地主机名、URL 凭据及本站；新增尾部点号的检查回归。
  HTTP 例外仅限本地 HTTP 127.0.0.1 入站到另一个 loopback origin，不开放云端 HTTP。
  这不是 DNS 重绑定防护或任意插件沙箱的保证。
- 初始化不再生成 `MODEL_ALLOWED_ORIGINS`，CLI 不再提供 `--model-origin`。
  预检兼容旧配置里的无效变量，但仍拒绝额外绑定和明文密码变量；
  重跑保留配置文件与加密密钥的字节，不要求普通用户手动迁移。
- 已从私有本地云端配置删除旧变量，保留原资源 ID 和密钥；本地 `.dev.vars` 没有修改。
  合成运行脚本不再注入允许列表；当前源码不读取这个变量。
- 全部 **174 项本地测试通过**，9 个修改后的源码/辅助脚本通过语法检查。
  新的 HTTPS 任意域名、Custom/反向代理 status 与 generate、旧空/非空/损坏变量、
  URL 拒绝规则、旧配置重跑与密钥保留均有回归。
- 普通构建/dry-run：337.32 KiB，gzip 81.40 KiB。
  已重建锁定 Helper/EJS 的 P3 资源：742 文件、77,356,347 字节；
  云端配置离线预检和带原 secret 的 dry-run 通过：338.11 KiB，gzip 81.43 KiB。
  预检仍明确返回 `cloudVerified=false`，没有把打包成功当作线上验收。
- 已构建 Worker 的 Miniflare 与直连 workerd 两条路径均实际联系本地模拟模型，
  首包前取消成功；每条路径 1 个请求，均未写出模型分片。
- 原版桌面/手机界面真实本地连接与发送通过，覆盖流式、非流式、重生成、停止、
  错误后手动重试、断流/缺少结束标记及恢复、手机重载。
  2 次模型列表请求与 23 次生成请求均面向本地合成服务，后端 Token 请求为 0。
- 23 份相同前端输入经 Workers 和锁定版原 ST 路由后，实际模型请求体相同；
  9 种 process API 模式相同。只有故意注入的 429 及其原版事件异常，
  桌面/手机截图已检查，手机没有横向溢出。

证据：`cloudflare/.build/generation-browser/results.json`、`parity.json`、
`desktop.png`、`mobile.png`，以及 `.build/cancellation-runtime/results.json`。
未重新跑完整 P3/MVU 流程，未联系真实模型，未改变云端资源或提交 Git。
已有线上实例仍含旧白名单，需另获所有者授权上传；本轮不声称用户此前的真实 403 已恢复。

## 白名单移除的云端更新

日期：2026-09-10。用户明确回复“更新”，授权更新现有测试 Worker。
本轮没有创建资源、迁移数据库、写入用户资料或调用真实模型。

- 上传前核实线上仍是首次部署版本；原 D1 UUID、私有 R2、workers.dev 和两项
  `secret_text` 绑定与记录一致。174 项本地测试及普通构建/dry-run 再次通过。
- 使用既有 P3 包：742 文件、77,356,347 字节。仅 1 个静态文件
  `/scripts/openai.js` 需要上传，741 个复用；另上传移除白名单后的 Worker 后端。
- 更新命令不带 `--secrets-file`，通过已声明的 `secrets.required` 继承原
  `AUTH_PASSWORD` 与 `DATA_KEY`，没有提交或输出密钥值；保留原 DB/FILES 绑定。
- 上传 338.11 KiB，gzip 81.43 KiB；工具报告 Startup Time 11 ms，
  这不是每请求 CPU 用量结论。部署状态再次只读确认新版本承接 100% 流量。
- 更新前后各 17 个真实 HTTPS 请求，均只针对已授权实例。
  现有 owner 登录、CSRF、跨来源保护、HTTP loopback 与指向本站的 URL 拒绝均通过。
- Custom 的 status 与 generate 使用一个不可能由正常凭据创建流程生成的
  非 UUID 测试凭据 ID。更新前返回 `MODEL_ORIGIN_NOT_ALLOWED`；
  更新后进入凭据检查并返回 `SECRET_NOT_FOUND`，明确证明不再因模型域名而拒绝。
  指定缺失凭据不会回退使用活动密钥，也不会发起提供商请求。
- OpenAI 反向代理使用空模型名的合成生成请求；更新后返回 `INVALID_MODEL`，
  证明通过地址检查后停在参数校验，尚未取凭据或发起出站请求。
- 线上 `/scripts/openai.js` 与已验证本地源码逐字节 SHA-256 相同，包含连接错误提示修复。
  两个锁定插件的 manifest 仍可读取；`P3-in-progress` 和 `readyForChat=false` 保持原状。
- 更新后再次核实原 D1、私有 Standard R2、无公开存储域名及两项 secret 绑定保留。
  没有打开用户聊天页面、读取其模型密钥明文或进行真实模型连接/生成验证。
  本轮没有重新跑云端完整浏览器/MVU 或存储写入回归，不扩大既有兼容与性能结论。

新旧版本 ID 与最新验证状态记录在私有
`cloudflare/.deploy/stworks-cloud-test/deployment.json`。
证据及可重复的实例专用检查脚本位于 `.build/cloud-model-origin-update/`：
`before.json`、`after.json`、`verify-update.mjs`。
不存在测试资料写入，因此无需清理角色卡、聊天、预设或密钥。

## P4 在线插件管理

日期：2026-09-10。用户要求补齐插件下载与安装。本轮仅修改和验证 `E:\STworks`，
没有部署云端、创建远程资源、推送 Git 或读取原项目的私人资料。

- 新增仓库解析、归档检查、D1/R2 版本管理与原路径资源分发；保留原版插件面板。
  支持公开 GitHub/GitLab，版本来自真实分支、标签或完整提交；不运行服务端插件代码。
- 原版安装、发现、版本、更新、分支切换和卸载接口接通；面板增加上一版回退图标。
  卸载失败会显示错误，不再继续保存/刷新并误报成功。
- 当前/上一版、清理账本和卸载记录通过本地并发测试；静态包更新、回退与删除也有专项测试。
  不确定 D1 提交结果时保留 R2 对象，避免误删实际生效的版本，孤立对象完整回收仍待实现。
- 全部 **199 项本地测试通过**，其中 P4 专项 25 项；前端与浏览器脚本语法检查通过。
- Node 22.14.0、Wrangler 4.129.1 普通构建/dry-run 通过：
  Worker 650.93 KiB，gzip 125.72 KiB；明确选择的资源 620 文件、39,881,807 字节，
  默认不含第三方插件，不将 Wrangler 扫描数量等同于资源清单。
- 实际测试 bundle SHA-256：
  `f615b2410c659b74a00a5d1cff638712295e4b710bdba93eeca952f62041acc9`。
- 修复仅在 workerd 出现的有界解压缓冲区问题：额外允许 64 KiB 内部输出余量，
  最终大小及 CRC 仍必须严格相同。真实两份归档与跨缓冲区边界的合成 JS 都通过。

最终真实网络轮次：
`cloudflare/.build/p4/913a954a-2db8-4a23-98b1-ad5af14e0a4e/results.json`。

- 隔离的 Miniflare/workerd、真实本地 D1/R2、随机登录凭据和端口，起始没有第三方插件。
  公开网络请求由本地测试传输层转发；不是 Cloudflare 边缘网络可达性验证。
- 原版弹窗真实下载并安装 Helper 4.9.5：
  提交 `8e0f4324e7d051025a333831411f03bd3145fac8`，ZIP 2,369,298 字节，
  SHA-256 `be7919088b9fdb0edf0544b683cdc23895823a463c7a8b36536ad517bdc5c07a`。
- 同样安装 EJS 1.17.9：
  提交 `d6f520d149aba146305b0b781ddd691d449c28d2`，ZIP 13,622,195 字节，
  SHA-256 `1a866075e0bf7499f4fb0c39e5e763077aeddfee851624b4464289176943546b`。
- 两者原编译模块与父窗口 API 加载，认证后的源码下载字节一致。
  固定提交查询和同提交更新不再重复访问远端；没有伪造一个可移动的分支。
- 移动分支使用明确标记的合成仓库，经原版更新按钮切换 v1/v2、回退后自动刷新，
  新建手机上下文加载正确代码。不是修改两个真实上游仓库来制造更新。
- Helper 合成全局变量在更新、回退、手机重载及卸载后仍保留。
  故意注入的一次卸载 503 显示错误、无成功提示、无刷新，旧插件仍存在；
  随后正常卸载、刷新后确认不再加载。
- 8 个阶段全部通过；页面异常 0，非预期 HTTP/控制台错误 0。
  唯一记录的 HTTP/控制台错误是上述故意注入的 503。
- 同目录桌面 1440x1000、手机 390x844 管理面板截图已经检查，
  新图标未覆盖其他操作，手机没有页面横向溢出。原版长名称省略显示保持不变。

同代码的归档回放轮次
`cloudflare/.build/p4/a516d8ed-434c-4c0b-a758-6005aa493da7/results.json`
也通过，明确标注 `local-pinned-archive-replay`，不能替代真实下载证据。
此前归档校验失败、测试刷新竞态与网络中断的失败目录保留，没有改写其结果。
可重复脚本为 `cloudflare/scripts/check-p4-local.mjs`，参数与边界见 `P4-CONTRACTS.md`。
脚本结束后关闭其浏览器和隔离运行时，没有改动既有本地或云端数据。

P4 继续保留云端 CPU/内存/免费额度、GitLab 完整真实安装、其他社区插件、
跨作用域 move、远端故障时回退入口显示和孤立对象恢复等未验收项。
本轮没有重跑完整 P3/MVU/模型生成流程，不关闭 P1/P2/P3 的剩余门槛；
当前云端实例仍是更新白名单后的旧版本，未获得 P4 新能力。

## P4 云端更新与资源检查

日期：2026-09-10。用户授权进入云端部署验收。只更新既有实例，不新建资源、
迁移数据库、更换密钥、修改订阅或推送 Git；没有调用模型或修改用户模型配置/聊天。

- 上传前 199 项测试、普通构建、保留两插件的资源构建与云端离线预检通过。
- 742 个静态文件、77,358,564 字节，实际上传两个变化文件、复用 740 个；
  Worker 651.84 KiB / gzip 125.75 KiB，工具报告启动时间 38 ms。
- 原 D1、R2 和两项 secret 绑定保留；控制面确认新版本承接 100% 流量。
  地址、版本 ID 和部署元数据保存在私有部署记录。
- 更新前后鉴权、插件 manifest 与前端哈希核对通过；新接口的 CSRF/来源保护通过。
- 两款插件真实边缘 GitHub 下载、global 安装、固定版本更新、父提交切换、回退通过。
- 临时切到同提交 R2 分发后，酒馆助手源码 ZIP、JS/CSS 与原字节相同。
  EJS 完整源码 ZIP 的客户端传输 120 秒超时；随后一次恢复请求遇到网络失败，
  已单独执行恢复并确认原 bundled 指针恢复。
- 单独核对已存 EJS 归档哈希后，EJS 的 631,922 字节运行 JS 通过实际 R2 分发及原文件 SHA-256 比较，
  测试结束后再次恢复 bundled；不把这个结果代替完整源码包下载通过。
- 两份 global 副本已删除，D1 的 current/previous 为空且待清理为 0。
  local 当前均是原 bundled 版本，各保留一个同提交 archive 上一版，不是无引用孤立对象。
- CPU 来自实际 GraphQL，而不是客户端耗时或未成功采集的 tail。
  测试窗口返回 39 个按秒统计桶、47 次请求、38 次 subrequest、平台错误 0；
  schema 确认 CPU 单位是微秒，最高桶 P99 为 247.021 ms。
  桶可能混入同期其他请求，不是一次请求的精确归因或全窗口 P99。
- 官方 Free HTTP CPU 预算为 10 ms；当前不能宣称免费资源门槛通过。
  账户订阅查询仍返回 403，未变更套餐；没有单请求内存测量。

综合脚本的原失败结果保留。恢复与 EJS 运行入口检查各有独立通过证据。
证据路径、耗时表、限制与下一阶段见 `P4-CLOUD.md`。
没有重新跑云端原版完整浏览器、MVU、真实模型或长时间压力测试；完整 P1/P2/P3/P5 不关闭。

## Actions 随包预装发布

日期：2026-09-10。用户明确调整当前优先级：先让 GitHub Actions 接替开发电脑执行
“准备插件、打包、部署”，在线安装后端优化留待后续细化。
本轮只改本地工作流、配套构建脚本和文档；没有推送、配置 GitHub Secrets、启动 Actions
或更新云端，没有新建资源、迁移数据库、读取用户聊天或更换密钥。

- 新增手动工作流 `STWorkers - Prepare Plugins and Deploy`，默认 `deploy=false`。
  只读仓库权限、固定 Action 提交、完整 Git 历史、关闭 checkout 凭据保留及并发取消。
  下载/构建步骤没有部署 Token；只有默认分支的显式部署步骤获得 Token。
- 215 项测试通过，新增 16 项专项；覆盖配置、手动触发、归档哈希/大小/重定向、
  绑定不匹配、泄密和链接路径、继承密钥与上传后检查失败的诚实报告。
  Cloudflare 部署守卫使用合成 API 响应和替代命令执行器，不是真实云端调用。
- 实际从公开 GitHub 下载 Helper 4.9.5 和 EJS 1.17.9 的固定归档，
  两项 SHA-256 与 P3/P4 原基线一致；复用 `packagePlugins`，不执行插件构建脚本。
- 生成 742 文件、77,358,564 字节的原版带插件资源；带插件 Actions dry-run 通过：
  Worker 651.84 KiB，gzip 125.75 KiB。普通无插件构建 dry-run 也通过：
  Worker 650.93 KiB，gzip 125.72 KiB。不是运行 CPU 或免费额度证明。
- 产物回执位于 `cloudflare/.build/actions/release.json`，明确为
  `local-build-and-dry-run-only`，`cloudVerified=false`。
- `check-actions-local.mjs` 用该 Worker 产物、全新内存 D1/R2 与随机密码，
  验证登录保护、两插件 manifest、原 JS/CSS 和完整源码字节一致。
- 原版桌面 1440x1000、全新手机上下文 390x844 直接加载 Helper 和 EJS；
  合成全局变量保存、桌面刷新与手机恢复通过，插件面板截图已检查，无横向溢出。
- 此验收路径的在线安装/更新请求 0、Worker 出站下载 0、R2 对象 0、
  D1 插件归档记录 0、页面异常 0、HTTP 错误 0。
- 浏览器与隔离 workerd 均已退出；没有调用模型或重跑完整 MVU/角色卡流程。

最终浏览器证据：
`cloudflare/.build/actions-check/1b63dfbb-e4ef-4e85-84ea-007a8f1315bb/results.json`。
Worker 产物 SHA-256：
`cfdf037f311369ed57788297cd0301b0177e6b26c9e590e2849d897458283b03`。
此前相同产物的功能通过记录
`cloudflare/.build/actions-check/e01c2f00-725a-4e1e-811d-14c9cf6fe639/results.json`
也保留；最终轮次补上等待抽屉动画结束后的插件面板截图。

这不是 GitHub 托管 Linux runner 的实际运行证明。首次创建资源、密钥初始化、自动迁移、
真实 Actions 上传、许可/免费额度、现有安装记录跨随包版本迁移均仍未验收。
当前预装版本未变，工作流不修改 D1 插件指针或覆盖在线安装/卸载记录。
完整使用步骤和边界见 `GITHUB-ACTIONS.md`。

## 待验证

- 原版前端完整初始化和所有核心编辑流程；当前只有记录的合成资料路径通过。
- 替换临时估算方案后的误差/性能；真实 Token ID 编解码需求与生态用例。
- 真实的长聊天分块/性能、跨存储崩溃回收、恢复工具及多记录并发删除。
- 酒馆助手/EJS 的其他复杂联动、非流式/未完整 SSE 停顿的取消边界、角色切换、导出再导入；
  灯塔 T/N 的真模型自主填表、其他事件竞态、变量清理后恢复、其他社区卡/MVU、
  其他动态模块与长期 iframe 资源回收。
- 插件再分发许可审查、完整外部依赖策略，以及 P4 云端整包传输、高 CPU 路径和其他社区扩展验收。
- 更多独立前端复杂样例、临界 Token 边界、非幂等宏及社区变量流程；真实模型接口。
- 当前账号账单套餐与免费额度；首次独立部署、基础存储及首屏已有上述云端证据，
  不能替代完整云端、真实模型或资源消耗验收。
- 实际 GitHub 托管 Actions 的依赖安装、插件拉取、云端 Token 权限、更新与运行验收；
  当前只有本地同脚本构建和隔离浏览器证据。

上述状态必须逐项更新，不能用前一阶段的通过推断后一阶段通过。

## 当前本地服务

以下为之前验证使用的预览参数；2026-09-10 本轮未启动服务，也未确认这些端口当前在线。

开发服务：`http://127.0.0.1:8788`，阶段信息位于 `/api/stworks/status`。
用户名 `owner`，密码见本地 `cloudflare/.dev.vars`，不要提交或分享该文件。
独立验收实例：`http://127.0.0.1:8789`，存储位于 `.wrangler/p1-test`。
用户名相同，两实例共享本地认证配置，但 D1/R2 数据目录分离。
独立 P3 插件预览：`http://127.0.0.1:8795`，使用 `.wrangler/p3-preview` 与 `.build/assets-p3`，
同一所有者认证配置，不使用浏览器回归的合成模型或测试数据。
当前源码推进 P4，基础生成与锁定版真实插件首轮本地合成验证已通过；完整 P1/P2/P3 尚未验收，
不应放入唯一一份个人数据。当前源码直接使用原版界面填写的 HTTPS 模型服务，
无需配置 `MODEL_ALLOWED_ORIGINS`；已有服务若尚未重启/重新构建，仍可能运行旧版代码。
新增 `DATA_KEY` 仅附加到新项目的 `.dev.vars`，没有覆盖已有 AUTH_PASSWORD。
Wrangler 的本地调试入口不属于生产应用接口；开发服务必须保持仅监听回环地址。
